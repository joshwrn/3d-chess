/*
Auto-generated by: https://github.com/pmndrs/gltfjsx
Command: npx gltfjsx@6.0.9 -t public/queen.gltf
*/

import type { FC } from 'react'
import React from 'react'

import { useGLTF } from '@react-three/drei'
import type * as THREE from 'three'
import type { GLTF } from 'three-stdlib'

import type { ModelProps } from '.'
import { MeshWrapper, PieceMaterial } from '.'

type GLTFResult = GLTF & {
  nodes: {
    Object001003: THREE.Mesh
  }
  materials: {
    [`Object001_mtl.003`]: THREE.MeshStandardMaterial
  }
}

export const QueenComponent: FC<ModelProps> = ({
  movingTo,
  finishMovingPiece,
  newTileHeight,
  isSelected,
  pieceIsBeingReplaced,
  wasSelected,
  ...props
}) => {
  const { nodes } = useGLTF(`/queen.gltf`) as unknown as GLTFResult
  return (
    <group {...props} dispose={null}>
      <MeshWrapper
        geometry={nodes.Object001003.geometry}
        movingTo={movingTo}
        finishMovingPiece={finishMovingPiece}
        newTileHeight={newTileHeight}
        isSelected={isSelected}
        pieceIsBeingReplaced={pieceIsBeingReplaced}
        wasSelected={wasSelected}
      >
        <PieceMaterial color={props.color} isSelected={isSelected} />
      </MeshWrapper>
    </group>
  )
}

useGLTF.preload(`/queen.gltf`)
